[project]
name = "ast-driven-data-augmentation"
version = "0.1.0"
description = "Schema-guided SQL AST augmentation with LLM-based question adaptation for Text-to-SQL"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "sqlglot>=30.2.1",
    "python-dotenv>=1.0.0",
    "openai>=1.0.0",
    "numpy<2",
    "openpyxl>=3.1.5",
    "pyyaml>=6.0.3",
]
